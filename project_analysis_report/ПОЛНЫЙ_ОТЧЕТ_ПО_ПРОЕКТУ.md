# ПОЛНЫЙ ОТЧЕТ ПО АНАЛИЗУ ПРОЕКТА FSM_Graph_Viz

**Дата анализа:** 2024  
**Версия проекта:** 1.0  
**Назначение проекта:** Анализ SystemVerilog кода с детектированием и визуализацией конечных автоматов (FSM)

---

## ОГЛАВЛЕНИЕ

1. [Общее описание проекта](#общее-описание-проекта)
2. [Архитектура проекта](#архитектура-проекта)
3. [Классификация файлов](#классификация-файлов)
4. [Детальный анализ файлов](#детальный-анализ-файлов)
5. [Рекомендации по очистке проекта](#рекомендации-по-очистке-проекта)
6. [Зависимости между модулями](#зависимости-между-модулями)

---

## ОБЩЕЕ ОПИСАНИЕ ПРОЕКТА

Проект **FSM_Graph_Viz** представляет собой комплексную систему для анализа SystemVerilog кода с акцентом на автоматическое детектирование конечных автоматов (Finite State Machines, FSM) и их визуализацию.

### Основные возможности:
- Парсинг SystemVerilog кода в Concrete Syntax Tree (CST) с использованием библиотеки `pyslang`
- Построение Abstract Syntax Tree (AST) из CST
- Автоматическое детектирование FSM в модулях SystemVerilog
- Извлечение состояний, переходов и метаданных FSM
- Построение графов переходов FSM
- Визуализация FSM через GUI (Tkinter) и экспорт в HTML/SVG
- Генерация подробных аналитических отчетов

### Технологический стек:
- **Python 3.x** - основной язык разработки
- **pyslang** - библиотека для парсинга SystemVerilog
- **Tkinter** - GUI фреймворк для визуализации
- **GraphViz/DOT** - формат для описания графов (опционально)

---

## АРХИТЕКТУРА ПРОЕКТА

Проект построен по модульной архитектуре с четким разделением ответственности:

```
┌─────────────────────────────────────────────────────────┐
│                    ВХОДНЫЕ ДАННЫЕ                       │
│              (SystemVerilog файлы .sv)                  │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│              СЛОЙ ПАРСИНГА (CST)                        │
│              cst_service.py                             │
│  - build_cst_from_text()                                │
│  - Утилиты навигации по дереву                          │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│           СЛОЙ АБСТРАКЦИИ (AST)                         │
│  ┌──────────────────┐  ┌──────────────────────────┐   │
│  │ ast_service.py   │  │ test_free.py             │   │
│  │ (базовый AST)    │  │ (CompleteASTService)     │   │
│  └──────────────────┘  └──────────────────────────┘   │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│        СЛОЙ ДЕТЕКТИРОВАНИЯ FSM                          │
│  ┌──────────────────────────┐  ┌─────────────────────┐ │
│  │ fsm_detector_service.py  │  │ FindeENUM.py        │ │
│  │ (основной детектор)      │  │ (enum детектор)     │ │
│  └──────────────────────────┘  └─────────────────────┘ │
│  ┌──────────────────────────┐                          │
│  │ fsm_enum_candidates_cst  │                          │
│  │ (фильтрация кандидатов)  │                          │
│  └──────────────────────────┘                          │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│        СЛОЙ ВИЗУАЛИЗАЦИИ                                 │
│  ┌──────────────────────────┐  ┌─────────────────────┐ │
│  │ fsm_graph_builder.py     │  │ fsm_graph_viz.py    │ │
│  │ (построение графов)      │  │ (GUI визуализация)  │ │
│  └──────────────────────────┘  └─────────────────────┘ │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│        СЛОЙ ОТЧЕТНОСТИ                                  │
│  ┌──────────────────────────┐  ┌─────────────────────┐ │
│  │ delit1.py                │  │ sv_analyzer.py      │ │
│  │ (полные отчеты)          │  │ (CLI анализатор)    │ │
│  └──────────────────────────┘  └─────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

---

## КЛАССИФИКАЦИЯ ФАЙЛОВ

### ✅ ПОЛЕЗНЫЕ ФАЙЛЫ (Основной функционал)

#### Критически важные (ядро системы):
1. **cst_service.py** - Базовый сервис для работы с CST
2. **ast_service.py** - Базовый AST сервис
3. **test_free.py** (CompleteASTService) - Полный AST сервис
4. **fsm_detector_service.py** - Основной детектор FSM
5. **fsm_graph_builder.py** - Построение графов FSM

#### Важные утилиты:
6. **fsm_graph_viz.py** - GUI визуализатор FSM
7. **sv_analyzer.py** - CLI анализатор SystemVerilog
8. **fsm_checker.py** - Утилита проверки FSM
9. **delit1.py** - Генератор полных аналитических отчетов
10. **run_complete_analysis.py** - Скрипт запуска полного анализа

#### Вспомогательные модули:
11. **FindeENUM.py** - Детектор enum переменных
12. **fsm_enum_candidates_cst.py** - Фильтрация enum кандидатов для FSM

### ⚠️ УСТАРЕВШИЕ/ДУБЛИКАТЫ (Рекомендуется удалить)

1. **ast_service_improved.py** - Улучшенная версия AST, но функционал уже есть в CompleteASTService
2. **ast_serviceFSM.py** - Старая версия с FSM, заменена на fsm_detector_service.py
3. **the_best.py** - Экспериментальный/дублирующий файл
4. **fsm_detector_gui.py** - Альтернативный GUI, дублирует fsm_graph_viz.py
5. **fsm_gui_app.py** - Еще один вариант GUI (дубликат)

### 🧪 ТЕСТОВЫЕ/ОТЛАДОЧНЫЕ ФАЙЛЫ (Полезны для разработки)

1. **test_fsm_graph_builder.py** - Тесты для fsm_graph_builder
2. **test_improved_ast.py** - Тесты для улучшенного AST
3. **sv_analyzer_testbench.py** - Большой тестовый стенд с метриками
4. **TESTENUM.py** - Тест для enum детектора
5. **debag.py** - Отладочный скрипт (можно удалить после отладки)
6. **delit2.py** - Возможно дубликат delit1.py или экспериментальный

### 📄 ПРИМЕРЫ/ДАННЫЕ

1. **03_01_detect_sequence_using_fsm.sv** - Пример SystemVerilog с FSM
2. **fsm_example8_fake_state.sv** - Тестовый пример (fake FSM)
3. **f1.sv** - Дополнительный пример

### ❓ НЕОПРЕДЕЛЕННЫЕ

1. **diagnostics_service.py** - Не прочитан, требуется дополнительный анализ

---

## ДЕТАЛЬНЫЙ АНАЛИЗ ФАЙЛОВ

### 1. cst_service.py ⭐⭐⭐⭐⭐

**Назначение:** Базовый сервис для построения и навигации по Concrete Syntax Tree (CST) из SystemVerilog кода.

**Ключевые функции:**
- `build_cst_from_text(source_text, filename)` - Построение CST из текста SystemVerilog
- `kind(node)` - Получение типа узла CST
- `children(node)` - Получение дочерних узлов
- `find_first(node, kind)` - Поиск первого узла заданного типа
- `find_all(node, kind)` - Поиск всех узлов заданного типа
- `text_of(node)` - Получение текстового представления узла
- `first_identifier_text(node)` - Извлечение первого идентификатора
- `collect_identifiers_inline(node)` - Сбор всех идентификаторов в узле
- `range_width_text(node)` - Извлечение ширины диапазона

**Зависимости:** pyslang

**Статус:** ✅ КРИТИЧЕСКИ ВАЖЕН - основа всего проекта

---

### 2. ast_service.py ⭐⭐⭐⭐⭐

**Назначение:** Базовый сервис для построения Abstract Syntax Tree (AST) из CST.

**Ключевые функции:**
- `build_ast_from_cst(tree)` - Построение базового AST из CST
- `_parse_module(mod_decl)` - Парсинг объявления модуля
- `_parse_interface(node)` - Парсинг интерфейса
- `_parse_package(node)` - Парсинг пакета
- `_parse_class(node)` - Парсинг класса
- `_parse_ports(header, mod)` - Парсинг портов модуля
- `_parse_parameter_port_list(header, mod)` - Парсинг параметров
- `_parse_data_declarations(mod_decl, mod)` - Парсинг объявлений данных
- `_parse_instantiations(mod_decl, mod)` - Парсинг инстансов модулей
- `_parse_assigns(mod_decl, mod)` - Парсинг непрерывных присваиваний
- `_parse_always(mod_decl, mod)` - Парсинг always блоков
- `_parse_initial(mod_decl, mod)` - Парсинг initial блоков
- `_parse_generate(mod_decl, mod)` - Парсинг generate блоков
- `_collect_types(root)` - Сбор информации о типах (typedef, struct, enum)
- `_build_connection_graph(modules, connections)` - Построение графа соединений
- `_build_module_hierarchies(modules, connections)` - Построение иерархий модулей
- `print_unified_ast(ast)` - Печать AST в читаемом формате

**Зависимости:** cst_service.py

**Статус:** ✅ КРИТИЧЕСКИ ВАЖЕН - базовый слой абстракции

---

### 3. test_free.py (CompleteASTService) ⭐⭐⭐⭐⭐

**Назначение:** Полный сервис для построения расширенного AST с нормализацией под формат FSMDetectorService.

**Ключевые функции:**
- `build_complete_ast_from_cst(tree)` - Построение полного AST с расширениями
- `_enhance_ast_with_pyslang(base_ast, tree)` - Расширение базового AST информацией от pyslang
- `_normalize_for_fsm_detector(ast)` - Нормализация AST под формат FSMDetectorService
- `_enhance_modules_with_pyslang(base_modules, root)` - Расширение информации о модулях
- `_find_and_parse_interfaces(root)` - Поиск и парсинг интерфейсов
- `_find_and_parse_packages(root)` - Поиск и парсинг пакетов
- `_find_and_parse_classes(root)` - Поиск и парсинг классов
- `_find_and_parse_programs(root)` - Поиск и парсинг program блоков
- `_find_and_parse_checkers(root)` - Поиск и парсинг checker блоков
- `_find_and_parse_configs(root)` - Поиск и парсинг config блоков
- `_collect_types(root)` - Сбор всех типов данных (typedef, struct, enum, union)
- `_collect_functions_tasks(root)` - Сбор функций и задач
- `_collect_parameters(root)` - Сбор параметров и defparams
- `_collect_preprocessor_directives(root)` - Сбор препроцессорных директив
- `_attach_enums_to_modules(modules, enums)` - Привязка enum'ов к модулям
- `_build_fsm_analysis(modules, enums)` - Построение FSM-метаданных
- `_build_complete_connection_graph(modules, interfaces)` - Построение полного графа соединений
- `_build_complete_hierarchies(modules, interfaces, connections)` - Построение полных иерархий
- `_analyze_clock_domains(modules)` - Анализ тактовых доменов
- `_analyze_reset_signals(modules)` - Анализ сигналов сброса
- `_analyze_assignments(modules)` - Анализ присваиваний
- `_analyze_timing(modules)` - Анализ временных характеристик
- `save_fsm_detector_input(ast, filepath, module_name, pretty)` - Сохранение модуля в формате FSMDetectorService
- `load_fsm_detector_input(filepath)` - Загрузка сохраненного модуля
- `print_complete_ast(ast)` - Печать полного AST

**Зависимости:** ast_service.py, cst_service.py

**Статус:** ✅ КРИТИЧЕСКИ ВАЖЕН - основной сервис для работы с полным AST

---

### 4. fsm_detector_service.py ⭐⭐⭐⭐⭐

**Назначение:** Основной сервис для детектирования конечных автоматов (FSM) в SystemVerilog модулях.

**Ключевые функции:**
- `detect_finite_state_machines(module, tree)` - Главная функция детектирования FSM
- `_init_fsm_patterns()` - Инициализация паттернов для детектирования FSM
- `_find_state_variables_extended(module)` - Расширенный поиск переменных состояний
- `_heuristic_state_analysis(module)` - Эвристический анализ для поиска переменных состояний
- `_is_state_like_variable(var_name, code_text)` - Проверка, является ли переменная похожей на состояние
- `_get_variable_case_context(var_name, code_text)` - Получение контекста использования переменной в case
- `_is_register_variable(var_name, module)` - Проверка, является ли переменная регистром
- `_analyze_fsm_structure(module, always_blocks, state_vars, state_registers)` - Расширенный анализ структуры FSM
- `_extract_states_extended(module)` - Расширенное извлечение состояний
- `_parse_enum_values(enum_definition)` - Парсинг значений из enum определения
- `_extract_states_from_case_statements_extended(module)` - Извлечение состояний из case statements
- `_extract_states_from_assignments_extended(module)` - Извлечение состояний из присваиваний
- `_heuristic_state_search(module)` - Эвристический поиск состояний
- `_remove_duplicate_states(states)` - Удаление дубликатов состояний
- `_extract_transitions_extended(always_blocks, state_vars, state_registers, states)` - Расширенное извлечение переходов
- `_is_state_assignment(left_var, state_vars, state_registers)` - Проверка, является ли присваивание присваиванием состояния
- `_analyze_state_transition(assignment, always_block, states, conditions)` - Анализ перехода состояния
- `_infer_from_state_extended(always_block, conditions)` - Расширенный вывод исходного состояния
- `_extract_state_from_expression_extended(expression, states)` - Расширенное извлечение состояния из выражения
- `_extract_transition_condition_extended(always_block, conditions)` - Расширенное извлечение условия перехода
- `_determine_fsm_type_extended(always_blocks, state_vars, state_registers, transitions)` - Расширенное определение типа FSM
- `_find_reset_and_clock_extended(always_blocks, module)` - Расширенный поиск сброса и тактового сигнала
- `_analyze_state_and_output_logic(always_blocks, state_vars, state_registers)` - Анализ логики следующего состояния и выходов
- `_calculate_fsm_complexity(fsm_info)` - Расчет сложности FSM
- `_determine_fsm_structure(fsm_info)` - Определение структуры FSM
- `_get_module_code_text(module)` - Получение текстового представления кода модуля

**Зависимости:** cst_service.py (опционально для tree)

**Статус:** ✅ КРИТИЧЕСКИ ВАЖЕН - центральный компонент детектирования FSM

---

### 5. fsm_graph_builder.py ⭐⭐⭐⭐⭐

**Назначение:** Построение графов переходов FSM из CST для визуализации.

**Ключевые функции:**
- `build_fsm_graphs_from_cst(tree)` - Главная функция построения графов FSM
- `_collect_scope_nodes(root)` - Построение карты scope_name -> узел scope'а
- `_collect_always_nodes(scope_node)` - Сбор всех always-конструкций внутри scope
- `_safe_text(node)` - Удобный хелпер для получения текста узла
- `_is_clocked_always(node)` - Проверка, что always-тело тактируемое
- `_is_comb_always(node)` - Проверка, что always-комбинационный
- `_var_written_in_always(node, var_name)` - Проверка, записывается ли переменная в always
- `_choose_state_and_next(scope_node, vars_in_group)` - Выбор основной переменной состояния и next-state
- `_find_case_nodes_on_state(scope_node, state_var)` - Поиск case-конструкций вида case(state_var)
- `_build_transitions_from_cases(case_nodes, state_var, next_state_var, enum_members)` - Построение переходов из case-блоков
- `_find_first_enum_in_text(enum_members, text)` - Поиск первого enum-элемента в строке
- `_find_assignments_with_conditions(text, lhs_name, enum_members)` - Поиск присваиваний с условиями
- `_detect_reset_state(always_nodes, state_var, enum_members)` - Определение reset-состояния
- `fsm_graph_to_dot(graph)` - Преобразование FSM-графа в строку формата DOT (GraphViz)
- `fsm_graphs_to_dot(graphs)` - Объединение нескольких FSM-графов в один DOT

**Зависимости:** cst_service.py, fsm_enum_candidates_cst.py, FindeENUM.py

**Статус:** ✅ КРИТИЧЕСКИ ВАЖЕН - построение графов для визуализации

---

### 6. fsm_graph_viz.py ⭐⭐⭐⭐

**Назначение:** GUI-приложение для визуализации FSM-графов, построенных из SystemVerilog-кода.

**Ключевые функции:**
- `fsm_graph_to_svg(graph, width, height)` - Преобразование FSM-графа в SVG-строку
- `fsm_graph_to_html(graph, title, width, height)` - Обертка SVG FSM-графа в HTML-документ
- `_escape_html(s)` - Простое экранирование для HTML
- `_normalize_cond(cond)` - Подчистка строки условия
- `FSMGuiApp` (класс) - Главный класс GUI приложения
  - `__init__()` - Инициализация GUI
  - `_create_menu()` - Создание меню
  - `_fill_example_code()` - Заполнение примера кода
  - `menu_open_sv()` - Открытие .sv файла
  - `parse_from_editor()` - Парсинг из редактора
  - `parse_sv_text(sv_text, filename_hint)` - Парсинг SystemVerilog текста
  - `_update_fsm_listbox()` - Обновление списка FSM
  - `on_fsm_select(event)` - Обработка выбора FSM
  - `get_current_graph()` - Получение текущего графа
  - `redraw_current_graph()` - Перерисовка текущего графа
  - `update_details_panel()` - Обновление панели деталей
  - `export_current_graph_as_html()` - Экспорт текущего графа как HTML

**Зависимости:** cst_service.py, fsm_graph_builder.py, tkinter

**Статус:** ✅ ВАЖЕН - пользовательский интерфейс для визуализации

---

### 7. FindeENUM.py ⭐⭐⭐

**Назначение:** Детектор enum-переменных в CST.

**Ключевые функции:**
- `detect_enum_variables_from_cst(tree)` - Главная функция детектирования enum переменных
  - Извлекает переменную name, enum type name, enum members, scope, file position
  - Обрабатывает как `typedef enum`, так и inline enum декларации

**Зависимости:** cst_service.py

**Статус:** ✅ ПОЛЕЗЕН - вспомогательный модуль для детектирования enum

---

### 8. fsm_enum_candidates_cst.py ⭐⭐⭐

**Назначение:** Фильтрация enum-переменных для выбора только тех, которые похожи на FSM-состояния.

**Ключевые функции:**
- `detect_fsm_enum_candidates_from_cst(tree)` - Главная функция фильтрации FSM-кандидатов
  - Использует `detect_enum_variables_from_cst` для получения всех enum-переменных
  - Применяет эвристики для отбора FSM-подобных переменных:
    - var_name содержит 'state' ИЛИ enum_name содержит 'state'
    - ИЛИ переменная встречается в case/unique case/priority case
    - ИЛИ используется в always-блоках с posedge/negedge
  - Возвращает список с дополнительным полем 'fsm_reason'

**Зависимости:** cst_service.py, FindeENUM.py

**Статус:** ✅ ПОЛЕЗЕН - фильтрация кандидатов для FSM

---

### 9. sv_analyzer.py ⭐⭐⭐⭐

**Назначение:** Утилита для анализа SystemVerilog-кода через CLI.

**Ключевые функции:**
- `analyze_systemverilog_code(source_text, filename, use_complete_ast)` - Построение AST по коду SystemVerilog
- `analyze_file(path, use_complete_ast)` - Анализ одного файла SystemVerilog по пути
- `save_json(data, out_path, pretty)` - Сохранение dict в JSON-файл
- `export_fsm_detector_payload(ast, out_path, module_name, pretty)` - Сохранение модуля в формате FSMDetectorService
- `print_ast_report(ast, filename, use_complete_ast, verbose_internal)` - Печать "человеческого" отчёта по AST
- `main()` - CLI-интерфейс с аргументами командной строки

**Зависимости:** cst_service.py, ast_service.py, test_free.py (CompleteASTService)

**Статус:** ✅ ВАЖЕН - CLI утилита для анализа

---

### 10. fsm_checker.py ⭐⭐⭐

**Назначение:** Утилита для проверки наличия конечного автомата (FSM) в SystemVerilog-модуле.

**Ключевые функции:**
- `detect_fsm_in_file(filepath, module_name)` - Запуск полного пайплайна детектирования FSM
- `print_fsm_result(result)` - Красивый вывод результата FSM-анализа в консоль
- `save_result_json(result, out_path)` - Сохранение результата FSM-анализа в JSON
- `main()` - CLI-интерфейс

**Зависимости:** cst_service.py, test_free.py (CompleteASTService), fsm_detector_service.py

**Статус:** ✅ ПОЛЕЗЕН - утилита проверки FSM

---

### 11. delit1.py ⭐⭐⭐⭐

**Назначение:** Генератор полных аналитических отчетов по AST.

**Ключевые функции:**
- `CompleteASTAnalysisReport` (класс) - Класс для генерации полного отчета
  - `__init__(ast_data)` - Инициализация с AST данными
  - `_init_fsm_patterns()` - Инициализация паттернов FSM
  - `generate_complete_report(output_file)` - Генерация полного отчета
  - `_print_general_statistics()` - Печать общей статистики
  - `_print_metadata()` - Печать метаданных
  - `_print_modules_detailed()` - Детальная информация о модулях
  - `_print_all_typedefs()` - Печать всех typedef
  - `_print_all_structs()` - Печать всех struct
  - `_print_all_enums_detailed()` - Печать всех enum
  - `_print_all_unions()` - Печать всех union
  - `_print_all_interfaces()` - Печать всех интерфейсов
  - `_print_all_packages()` - Печать всех пакетов
  - `_print_all_classes()` - Печать всех классов
  - `_print_all_programs()` - Печать всех program блоков
  - `_print_all_checkers()` - Печать всех checker блоков
  - `_print_all_configs()` - Печать всех config блоков
  - `_print_all_functions()` - Печать всех функций
  - `_print_all_tasks()` - Печать всех задач
  - `_print_all_parameters()` - Печать всех параметров
  - `_print_enhanced_fsm_analysis()` - Расширенный анализ FSM
  - `_print_connection_graph()` - Печать графа соединений
  - `_print_hierarchies()` - Печать иерархий модулей
  - `_print_timing_analysis()` - Печать таймингового анализа
  - `_print_reset_analysis()` - Печать анализа сигналов сброса
  - `_print_assignment_analysis()` - Печать анализа присваиваний
- `generate_complete_analysis_report(ast_data, output_file, console_output)` - Утилита для генерации отчета
- `save_ast_json(ast_data, output_file)` - Сохранение AST в JSON

**Зависимости:** test_free.py (CompleteASTService), fsm_detector_service.py

**Статус:** ✅ ВАЖЕН - генерация подробных отчетов

---

### 12. run_complete_analysis.py ⭐⭐⭐

**Назначение:** Скрипт для запуска полного анализа SystemVerilog кода.

**Ключевые функции:**
- `analyze_your_code()` - Функция анализа кода с примером SystemVerilog
  - Строит CST
  - Строит полный AST
  - Генерирует полный аналитический отчет
  - Сохраняет данные AST в JSON

**Зависимости:** cst_service.py, test_free.py (CompleteASTService), delit1.py

**Статус:** ✅ ПОЛЕЗЕН - скрипт для демонстрации работы системы

---

### 13. ast_service_improved.py ⚠️

**Назначение:** Улучшенная версия AST сервиса с улучшенным детектированием FSM.

**Проблема:** Функционал уже реализован в `test_free.py` (CompleteASTService) и `fsm_detector_service.py`. Является дубликатом/устаревшей версией.

**Статус:** ❌ УСТАРЕВШИЙ - рекомендуется удалить или заменить на CompleteASTService

---

### 14. ast_serviceFSM.py ⚠️

**Назначение:** Старая версия AST сервиса с FSM детектированием.

**Проблема:** Заменена на `fsm_detector_service.py` и `test_free.py` (CompleteASTService).

**Статус:** ❌ УСТАРЕВШИЙ - рекомендуется удалить

---

### 15. the_best.py ⚠️

**Назначение:** Исправленный сервис для работы с AST (ASTServiceFixed).

**Проблема:** Похоже на экспериментальный файл или дубликат функционала из других файлов.

**Статус:** ❌ ДУБЛИКАТ/ЭКСПЕРИМЕНТАЛЬНЫЙ - рекомендуется удалить

---

### 16. fsm_detector_gui.py ⚠️

**Назначение:** Графический интерфейс для детектирования FSM.

**Проблема:** Дублирует функционал `fsm_graph_viz.py`, но с другим подходом (фокус на детектировании, а не визуализации графов).

**Статус:** ❌ ДУБЛИКАТ - рекомендуется использовать fsm_graph_viz.py или объединить функционал

---

### 17. fsm_gui_app.py ⚠️

**Назначение:** GUI-приложение для визуализации FSM-графов (альтернативная версия).

**Проблема:** Дублирует функционал `fsm_graph_viz.py`.

**Статус:** ❌ ДУБЛИКАТ - рекомендуется использовать fsm_graph_viz.py

---

### 18. test_fsm_graph_builder.py 🧪

**Назначение:** Набор автотестов для `fsm_graph_builder.build_fsm_graphs_from_cst`.

**Ключевые функции:**
- `normalize_cond(cond)` - Нормализация условия для сравнения
- `transitions_to_set(transitions)` - Преобразование переходов к множеству кортежей
- `check_graph(actual, expected)` - Сравнение реального графа с ожидаемым
- `run_test(name, sv_code, expected_graphs)` - Запуск одного теста
- `main()` - Запуск всех тестов

**Тестовые примеры:** 12 различных тестовых случаев FSM

**Статус:** ✅ ПОЛЕЗЕН ДЛЯ РАЗРАБОТКИ - тесты для fsm_graph_builder

---

### 19. test_improved_ast.py 🧪

**Назначение:** Тестирование улучшенного детектирования FSM.

**Ключевые функции:**
- `test_moore_machine()` - Тест автомата Мура с полным детектированием
- `main()` - Основная функция тестирования

**Проблема:** Использует `ast_serviceFSM`, который устарел.

**Статус:** ⚠️ ТРЕБУЕТ ОБНОВЛЕНИЯ - нужно переписать на использование fsm_detector_service.py

---

### 20. sv_analyzer_testbench.py 🧪

**Назначение:** Большой тестовый стенд для sv_analyzer и FSMDetectorService с метриками качества.

**Ключевые функции:**
- `FSMTestCase` (dataclass) - Модель тест-кейса
- `compute_regression_metrics(y_true, y_pred)` - Расчет MAE, MSE, RMSE, R²
- `compute_classification_metrics(y_true, y_pred)` - Расчет accuracy, precision, recall, F1
- `run_single_test(case)` - Запуск одного теста
- `run_all_tests()` - Запуск всех тестов с красивым отчетом и метриками

**Тестовые примеры:** 7 различных тестовых случаев FSM

**Статус:** ✅ ПОЛЕЗЕН ДЛЯ РАЗРАБОТКИ - большой тестовый стенд с метриками

---

### 21. TESTENUM.py 🧪

**Назначение:** Простой тест для детектора enum переменных.

**Ключевые функции:**
- Тестирование `detect_enum_variables_from_cst` на примере кода

**Статус:** ✅ ПОЛЕЗЕН ДЛЯ РАЗРАБОТКИ - простой тест

---

### 22. debag.py 🧪

**Назначение:** Отладочный скрипт для тестирования пайплайна анализа.

**Ключевые функции:**
- Демонстрация работы: CST -> AST -> FSM детектор

**Статус:** ⚠️ ОТЛАДОЧНЫЙ - можно удалить после завершения отладки

---

### 23. delit2.py ❓

**Назначение:** Не определено (файл не прочитан полностью).

**Статус:** ❓ ТРЕБУЕТ АНАЛИЗА

---

### 24. diagnostics_service.py ❓

**Назначение:** Не определено (файл не прочитан).

**Статус:** ❓ ТРЕБУЕТ АНАЛИЗА

---

### 25. 03_01_detect_sequence_using_fsm.sv 📄

**Назначение:** Пример SystemVerilog кода с FSM для детектирования последовательности "1010".

**Содержимое:**
- Модуль `detect_4_bit_sequence_using_fsm` - Moore FSM с enum состояниями
- Модуль `detect_6_bit_sequence_using_fsm` - заглушка для задачи

**Статус:** ✅ ПОЛЕЗЕН - тестовый пример

---

### 26. fsm_example8_fake_state.sv 📄

**Назначение:** Тестовый пример "fake FSM" - простой счетчик, который может быть ошибочно принят за FSM.

**Содержимое:**
- Модуль `fsm_example8_fake_state` - счетчик с именем `state_counter`

**Статус:** ✅ ПОЛЕЗЕН - тестовый пример для проверки качества детектора

---

### 27. f1.sv 📄

**Назначение:** Дополнительный пример SystemVerilog (не прочитан полностью).

**Статус:** ❓ ТРЕБУЕТ АНАЛИЗА

---

## РЕКОМЕНДАЦИИ ПО ОЧИСТКЕ ПРОЕКТА

### Файлы для удаления:

1. **ast_service_improved.py** - функционал есть в CompleteASTService
2. **ast_serviceFSM.py** - заменен на fsm_detector_service.py
3. **the_best.py** - экспериментальный/дублирующий файл
4. **fsm_detector_gui.py** - дублирует fsm_graph_viz.py
5. **fsm_gui_app.py** - дублирует fsm_graph_viz.py
6. **debag.py** - отладочный файл (можно удалить после завершения отладки)
7. **delit2.py** - если является дубликатом delit1.py

### Файлы для обновления:

1. **test_improved_ast.py** - переписать на использование fsm_detector_service.py вместо ast_serviceFSM

### Файлы для дополнительного анализа:

1. **delit2.py** - определить назначение
2. **diagnostics_service.py** - проанализировать и документировать
3. **f1.sv** - проанализировать содержимое

---

## ЗАВИСИМОСТИ МЕЖДУ МОДУЛЯМИ

```
cst_service.py
    │
    ├──> ast_service.py
    │       │
    │       └──> test_free.py (CompleteASTService)
    │               │
    │               ├──> fsm_detector_service.py
    │               │       │
    │               │       └──> fsm_checker.py
    │               │
    │               ├──> delit1.py
    │               │       │
    │               │       └──> run_complete_analysis.py
    │               │
    │               └──> sv_analyzer.py
    │
    ├──> FindeENUM.py
    │       │
    │       └──> fsm_enum_candidates_cst.py
    │               │
    │               └──> fsm_graph_builder.py
    │                       │
    │                       └──> fsm_graph_viz.py
    │
    └──> TESTENUM.py (тест)
```

---

## ИТОГОВАЯ ОЦЕНКА ПРОЕКТА

### Сильные стороны:
- ✅ Модульная архитектура с четким разделением ответственности
- ✅ Подробное детектирование FSM с множеством эвристик
- ✅ Полная поддержка SystemVerilog конструкций
- ✅ Удобные инструменты визуализации и отчетности
- ✅ Хорошее покрытие тестами

### Слабые стороны:
- ⚠️ Наличие дублирующих файлов (GUI варианты, устаревшие версии)
- ⚠️ Некоторые тесты используют устаревшие модули
- ⚠️ Не все файлы документированы

### Рекомендации:
1. Удалить устаревшие и дублирующие файлы
2. Обновить тесты для использования актуальных модулей
3. Добавить документацию для недостающих файлов
4. Рассмотреть объединение GUI вариантов в один универсальный интерфейс

---

**Конец отчета**

