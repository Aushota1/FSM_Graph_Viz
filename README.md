# FSM Graph Visualizer - Web Application

Веб-приложение для визуализации конечных автоматов (FSM) из SystemVerilog кода.

## Описание

Это веб-приложение предоставляет интерфейс для анализа и визуализации конечных автоматов, найденных в SystemVerilog коде. Приложение состоит из:

- **Backend API** (FastAPI) - использует существующие функции из `FSM_Graph_Viz` для парсинга и построения графов
- **Frontend** (React + ReactFlow) - современный веб-интерфейс для визуализации FSM

## Функциональность

### Основные возможности:

1. **Редактор кода** - ввод и редактирование SystemVerilog кода
2. **Парсинг** - автоматическое обнаружение FSM в коде
3. **Список FSM** - отображение всех найденных конечных автоматов
4. **Отдельная страница визуализации** - каждый граф открывается на отдельной странице
5. **Изменяемые размеры панелей** - все окна можно изменять по размеру (resizable panels)
6. **Визуализация графа** - интерактивная визуализация состояний и переходов с ReactFlow
7. **Детальная информация** - подробная информация о каждом FSM:
   - Scope, enum type, state variables
   - Список состояний
   - Таблица переходов с условиями
8. **Экспорт** - экспорт графа в HTML формат
9. **Сохранение состояния** - код и графы автоматически сохраняются в браузере
10. **Интерактивное редактирование** - добавление/удаление состояний и переходов
11. **Force-directed layout** - автоматическая раскладка узлов графа
12. **Генерация кода** - автоматическая генерация SystemVerilog кода из отредактированного графа

## Структура проекта

```
.
├── FSM_Graph_Viz/          # Исходные модули (не изменяются)
│   ├── cst_service.py
│   ├── fsm_graph_builder.py
│   ├── fsm_graph_viz.py
│   └── ...
├── backend/                # FastAPI бекенд
│   ├── main.py
│   ├── code_generator.py   # Генератор SystemVerilog кода
│   ├── requirements.txt
│   └── Dockerfile
├── frontend/               # React фронтенд
│   ├── src/
│   │   ├── App.jsx
│   │   ├── components/
│   │   │   ├── CodeEditor.jsx
│   │   │   ├── FSMList.jsx
│   │   │   ├── GraphVisualization.jsx
│   │   │   ├── EditableGraphVisualization.jsx  # Компонент редактирования
│   │   │   └── DetailsPanel.jsx
│   │   └── ...
│   ├── package.json
│   └── Dockerfile
├── docker-compose.yml      # Docker Compose конфигурация
└── README.md
```

## Установка и запуск

### Вариант 1: Docker Compose (рекомендуется)

1. Убедитесь, что Docker и Docker Compose установлены

2. Запустите приложение:
```bash
docker-compose up --build
```

3. Откройте браузер:
   - Frontend: http://localhost:3000
   - Backend API: http://localhost:8000
   - API документация: http://localhost:8000/docs

### Вариант 2: Локальный запуск

#### Backend

1. Перейдите в директорию backend:
```bash
cd backend
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Убедитесь, что модули из `FSM_Graph_Viz` доступны (они должны быть в родительской директории)

5. Запустите сервер:
```bash
python main.py
# или
uvicorn main:app --reload
```

#### Frontend

1. Перейдите в директорию frontend:
```bash
cd frontend
```

2. Установите зависимости:
```bash
npm install
```

3. Запустите dev сервер:
```bash
npm run dev
```

4. Откройте http://localhost:3000

## Использование

1. **Ввод кода**: Вставьте SystemVerilog код в редактор или используйте пример кода
2. **Парсинг**: Нажмите кнопку "Парсить" для анализа кода
3. **Выбор FSM**: Выберите FSM из списка слева - граф откроется на отдельной странице
4. **Изменение размеров**: Перетаскивайте разделители между панелями для изменения их размеров
5. **Просмотр графа**: Граф отображается на отдельной странице с возможностью зума и навигации
6. **Редактирование графа**: 
   - Нажмите кнопку "Редактирование" для перехода в режим редактирования
   - Добавляйте состояния: кнопка "+ Состояние"
   - Добавляйте переходы: выберите узел и нажмите "➡ Переход", затем кликните на целевой узел
   - Удаляйте элементы: выберите элемент и нажмите "🗑 Удалить" или клавишу Delete
   - Редактируйте условия переходов: выберите переход и нажмите "✏ Условие"
   - Устанавливайте reset-состояние: выберите узел и нажмите "⚡ Reset"
7. **Генерация кода**: В режиме редактирования нажмите "🔄 Сгенерировать код" для создания SystemVerilog кода
8. **Детали**: Переключитесь на вкладку "Детали" или "Переходы" для подробной информации
9. **Экспорт**: Нажмите "Экспорт HTML" для сохранения графа
10. **Навигация**: Используйте кнопку "Назад" для возврата на главную страницу

## API Endpoints

### POST /api/parse
Парсинг SystemVerilog кода и построение FSM-графов.

**Request:**
```json
{
  "code": "module ...",
  "filename": "source.sv"
}
```

**Response:**
```json
{
  "success": true,
  "graphs_count": 2,
  "graphs": [...],
  "parse_id": 1
}
```

### GET /api/graphs
Получить все графы из последнего парсинга.

### GET /api/graph/{graph_id}
Получить конкретный граф по ID.

### GET /api/export/{graph_id}/html
Экспорт графа в HTML формат.

### GET /api/export/{graph_id}/svg
Экспорт графа в SVG формат.

### POST /api/graph/{graph_id}/update
Обновить граф (для интерактивного редактирования).

**Request:**
```json
{
  "graph": {
    "scope": "...",
    "states": [...],
    "transitions": [...],
    ...
  }
}
```

### POST /api/graph/{graph_id}/generate-code
Генерировать SystemVerilog код из отредактированного графа.

**Response:**
```json
{
  "code": "module ..."
}
```

## Технологии

### Backend
- **FastAPI** - современный веб-фреймворк для Python
- **Uvicorn** - ASGI сервер
- **pyslang** - парсер SystemVerilog
- Существующие модули из `FSM_Graph_Viz`

### Frontend
- **React 18** - библиотека для создания UI
- **React Router** - роутинг для навигации между страницами
- **ReactFlow** - библиотека для визуализации графов
- **react-resizable-panels** - библиотека для изменения размеров панелей
- **Vite** - сборщик и dev-сервер
- **Axios** - HTTP клиент

## Важные замечания

1. **Логика не изменена**: Все функции парсинга и построения графов используются из исходных модулей `FSM_Graph_Viz` без изменений
2. **Хранилище**: В текущей реализации графы хранятся в памяти. Для продакшена рекомендуется использовать базу данных
3. **CORS**: Настроен для работы с фронтендом на localhost:3000 и localhost:5173

## Разработка

### Добавление новых функций

1. **Backend**: Добавьте новые эндпоинты в `backend/main.py`
2. **Frontend**: Создайте новые компоненты в `frontend/src/components/`

### Отладка

- Backend логи доступны в консоли или через Docker logs
- Frontend использует React DevTools для отладки
- API документация доступна по адресу http://localhost:8000/docs

## Лицензия

Проект использует существующие модули из `FSM_Graph_Viz`. Соблюдайте лицензию исходных модулей.

## Поддержка

При возникновении проблем:
1. Проверьте, что все зависимости установлены
2. Убедитесь, что модули `FSM_Graph_Viz` доступны
3. Проверьте логи в консоли/Docker

